import styled, { css } from "styled-components";

export const Container = styled.div`
  ${({ theme }) => css`
  background: linear-gradient(180deg, #181618 1.04%, #918F9B 82.29%, rgba(19, 18, 27, 0.9) 100%);
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    &&hover:

  `}
`;
